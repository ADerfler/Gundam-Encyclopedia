const timelines = require('../db/timelines/timelines.js')
module.exports = {
    getTimeline: (req, res) => {
        let timeline = timelines[req.params.id]
        res.render('uc.ejs', {
            timeline: timeline,
        })
    }
    
}